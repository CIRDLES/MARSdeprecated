import {
  SAMPLE_DETAIL_INITIAL_STATE,
  addSampleDetail
} from '../core/sampleDetailCore'
import {
  FETCH_SAMPLE_SUCCESS
} from '../actionTypes'

export default function sampleDetail(state = SAMPLE_DETAIL_INITIAL_STATE, action) {
  switch(action.type) {
    default:
      return state
  }
}

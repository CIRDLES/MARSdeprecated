import * as actions from '../actionTypes'

export function user_code(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_USERCODE,
    index,
    value,
    originalKey
  }
}

export function sample_type(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_SAMPLE_TYPE,
    index,
    value,
    originalKey
  }
}

export function name(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_NAME,
    index,
    value,
    originalKey
  }
}

export function sample_material(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_MATERIAL,
    index,
    value,
    originalKey
  }
}

export function igsn(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_IGSN,
    index,
    value,
    originalKey
  }
}

export function parent_igsn(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_PARENT_IGSN,
    index,
    value,
    originalKey
  }
}

export function is_private(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_IS_PRIVATE,
    index,
    value,
    originalKey
  }
}

export function publish_date(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_PUBLISH_DATE,
    index,
    value,
    originalKey
  }
}

export function classification(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_CLASSIFICATION,
    index,
    value,
    originalKey
  }
}

export function classification_comment(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_CLASSIFICATION_COMMENT,
    index,
    value,
    originalKey
  }
}

export function field_name(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_FIELD_NAME,
    index,
    value,
    originalKey
  }
}

export function description(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_DESCRIPTION,
    index,
    value,
    originalKey
  }
}

export function age_min(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_AGE_MIN,
    index,
    value,
    originalKey
  }
}

export function age_max(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_AGE_MAX,
    index,
    value,
    originalKey
  }
}

export function age_unit(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_AGE_UNIT,
    index,
    value,
    originalKey
  }
}

export function geological_age(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_GEOLOGICAL_AGE,
    index,
    value,
    originalKey
  }
}

export function geological_unit(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_GEOLOGICAL_UNIT,
    index,
    value,
    originalKey
  }
}

export function collection_method(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_COLLECTION_METHOD,
    index,
    value,
    originalKey
  }
}

export function collection_method_descr(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_COLLECTION_METHOD_DESCR,
    index,
    value,
    originalKey
  }
}

export function size(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_SIZE,
    index,
    value,
    originalKey
  }
}

export function size_unit(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_SIZE_UNIT,
    index,
    value,
    originalKey
  }
}

export function sample_comment(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_SAMPLE_COMMENT,
    index,
    value,
    originalKey
  }
}

export function purpose(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_PURPOSE,
    index,
    value,
    originalKey
  }
}

export function latitude(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LATITUDE,
    index,
    value,
    originalKey
  }
}

export function longitude(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LONGITUDE,
    index,
    value,
    originalKey
  }
}

export function latitude_end(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LATITUDE_END,
    index,
    value,
    originalKey
  }
}

export function longitude_end(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LONGITUDE_END,
    index,
    value,
    originalKey
  }
}

export function elevation(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_ELEVATION,
    index,
    value,
    originalKey
  }
}

export function elevation_end(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_ELEVATION_END,
    index,
    value,
    originalKey
  }
}

export function vertical_datum(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_VERTICAL_DATUM,
    index,
    value,
    originalKey
  }
}

export function northing(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_NORTHING,
    index,
    value,
    originalKey
  }
}

export function easting(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_EASTING,
    index,
    value,
    originalKey
  }
}

export function zone(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_ZONE,
    index,
    value,
    originalKey
  }
}

export function navigation_type(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_NAVIGATION_TYPE,
    index,
    value,
    originalKey
  }
}

export function primary_location_type(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_PRIMARY_LOCATION_TYPE,
    index,
    value,
    originalKey
  }
}

export function primary_location_name(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_PRIMARY_LOCATION_TYPE,
    index,
    value,
    originalKey
  }
}

export function location_description(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LOCATION_DESCRIPTION,
    index,
    value,
    originalKey
  }
}

export function locality(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LOCALITY,
    index,
    value,
    originalKey
  }
}

export function locality_description(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LOCALITY_DESCRIPTION,
    index,
    value,
    originalKey
  }
}

export function country(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_COUNTRY,
    index,
    value,
    originalKey
  }
}

export function province(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_PROVINCE,
    index,
    value,
    originalKey
  }
}

export function county(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_COUNTY,
    index,
    value,
    originalKey
  }
}

export function city(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_CITY,
    index,
    value,
    originalKey
  }
}

export function cruise_field_prgrm(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_CRUISE_FIELD_PRGRM,
    index,
    value,
    originalKey
  }
}

export function platform_type(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_PLATFORM_TYPE,
    index,
    value,
    originalKey
  }
}

export function platform_name(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_PLATFORM_NAME,
    index,
    value,
    originalKey
  }
}

export function platform_descr(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_PLATFORM_DESCR,
    index,
    value,
    originalKey
  }
}

export function launch_platform_name(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LAUNCH_PLATFORM_NAME,
    index,
    value,
    originalKey
  }
}

export function launch_id(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LAUNCH_ID,
    index,
    value,
    originalKey
  }
}

export function launch_type_name(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_LAUNCH_TYPE_NAME,
    index,
    value,
    originalKey
  }
}

export function collector(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_COLLECTOR,
    index,
    value,
    originalKey
  }
}

export function collector_detail(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_COLLECTOR_DETAIL,
    index,
    value,
    originalKey
  }
}

export function collection_start_date(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_COLLECTION_START_DATE,
    index,
    value,
    originalKey
  }
}

export function collection_end_date(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_COLLECTION_END_DATE,
    index,
    value,
    originalKey
  }
}

export function collection_date_precision(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_COLLECTION_DATE_PRECISION,
    index,
    value,
    originalKey
  }
}

export function current_archive(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_CURRENT_ARCHIVE,
    index,
    value,
    originalKey
  }
}

export function current_archive_contact(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_CURRENT_ARCHIVE_CONTACT,
    index,
    value,
    originalKey
  }
}

export function original_archive(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_ORIGINAL_ARCHIVE,
    index,
    value,
    originalKey
  }
}

export function depth_min(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_DEPTH_MIN,
    index,
    value,
    originalKey
  }
}

export function depth_max(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_DEPTH_MAX,
    index,
    value,
    originalKey
  }
}

export function depth_scale(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_DEPTH_SCALE,
    index,
    value,
    originalKey
  }
}

export function other_names(index, value, originalKey) {
  return {
    type: actions.UPLOADS_ACTION_PREFIX + actions.INIT_SAMPLE_OTHER_NAMES,
    index,
    value,
    originalKey
  }
}
